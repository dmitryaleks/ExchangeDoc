# docs/kosdaq/

KOSDAQ-specific topic write-ups.

One Markdown file per topic, named to match the entry in `../index.yaml`.
For topics where KOSDAQ rules are identical to KOSPI, prefer pointing to a
shared write-up in `../common/` rather than duplicating prose; document only
the KOSDAQ-specific deltas here.

See `../STYLE.md` (to be written in Phase 0.3) for the per-file template.
