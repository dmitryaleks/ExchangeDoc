# docs/common/

Topic write-ups that apply to both KOSPI and KOSDAQ without divergence, plus
cross-cutting documents (e.g. `comparison.md`, `introduction.md`).

Reference these from `../index.yaml` with `markets: [KOSPI, KOSDAQ]`.
